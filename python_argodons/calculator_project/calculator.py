import sys
from xmlrpc.client import METHOD_NOT_FOUND
from meth import *

class Calculator():
    str=sys.stdin.readline()
    if (not str.isnumeric()):
        match str:
            case '+':
                met
            case '-':
                break
            case '*':
                break
            case '/':
                break